version 0.0.0 release 6
	(adv) servidor desistalar fprot y vmware server

version 0.0.8 release 5
	(adv) se corriegen los errores en la web en la creacion de los formularios.
	(adv) se revisa toda la libreria Deploy.lib
	(adv) se modifica el ParseaSysprep para que solo admita ficherovariables int_disk_destinio int_parttiin_destino
	(adv) se modifica CreaePartitionFromPartition
	(adv) se elimina completamente el servicio EACtorrent y ctcs.

version 0.0.8 release 4
	(adv) se corrige bug del EACpxe
	(adv) ser revisa la web administrativa eac para comprobar el acceso a EAC.conf
	(adv) se inserta en el rc.local del servidor las instrucciones a realizar segun (arrnque live cd; primer arranque; normal)
	(adv) se revisan los /var/EAC/admin/EAC_conf_{server.sh eac.sh}

version 0.0.8.release 3 
	(adv) se instala en el servidor gimp y netpbm
	(adv) se separa el EACpxe del EACserver
	(adv) se corrige CreatePartition de ATA.lib  (creando el filysuynte)
	(adv) se corrige RestorePartitionFromImage de Deploy.lib (admite ntfsclone, partimage, multicas, unicast p2p
	(adv) servidor: se instala en el servidor el mysql workbench y /usr/sbin/mysql-gui-tools-5.0
	(adv) Servidor y cliente: se compila mbuffer ( (http://www.maier-komor.de/mbuffer.html), requiere apt-get install ksh.
	(adv) se cominza el RestorePartitionFromPartition

ver. 0.0.8 release 2
	(adv) se instala en cliente y servidr bfr y fbgrab
	(adv) se crea la funcion MountChrootEACclient y UmountChrootEACclient
	(adv) Se corrige RestorePartition.  OJO la info la obtiene del fichero *.mcast de la imagen.  -fase testeo-

ver. 0.0.8 release 1
	(adv) Se modifica el EACtorrent para que no se inicie automáticamente, sino que se iran cargando las imagenes bajo peticion de los clientes.
	(adv) Se modifica EACserver
		control de la transferenica por ctorrent 100%
		ofrezca el torrent bajo demanda.
		Se integra el gestor de boot. (personalizacion parametros kernel segun el hardware
		

	(adv) Se introduce EACclient
		gestion de log con controles de tiempo. * log para sistemas offilen /var/EAC/hosts/$IP-log 100% * tabla log para sistemas centralizados 90%
		gestion de hostname del cliente por variables y por fichero 

	(adv) funcion InfoHardware en ATA.lib para la gestion del inventario.
		* inventario para sistemas offline /var/EAC/hosts/$IP-InfoHard 100%
		* inventario para sistemas centralizados. 0%

	(adv) funcion infoCache en Filisystem.lib para la gestion de la cache de los clientes (tamaño, espacio libre y listado de imagenes)


	(adv) Web eac
		arranque wake on lan desde la pestaña equipos


ver. 0.0.7 release 7
	se corrige EAC_conf_srv.sh
ver. 0.0.7 release 6
	se corrige la ubicacion .bf del torrent tambien en la cache
	se adapata el update cache
ver. 0.0.7 release 5
-Servidor LINUX EAC
	(adv) software: se instala el ctcs (controlar parametros ctorrent desde web) http://www.rahul.net/dholmes/ctorrent/ctcs.html
	(adv) se configura el /etc/default/nfs-kernel-server con: RPCNFSDCOUNT=38 y RPCNFSDPRIORITY=-20
        (adv) se formatea el fichero INSTALL, se adapta el documento original
	guia rapida a la version 007r5
-/var/EAC/admin/procedimientos/EACtorrent
	(adv) con start se inicia tambien el ctcs
	(adv) con start y restart se modifica la linea    nice -n 8 /var/EAC/admin/procedimientos/EACtorrent start

-/var/EAC/admin/librerias/Deploy.lib
	(adv) UpdateCache en su llamada ctorrent se redirige a $2$portctcs  -> $IPservidor:2780
	(adv) se corrigen bugs de UpdateCache
	(adv) se crea la funcion OpenArchiveZIP()
-/var/EAC/admin/librerias/Boot.lib
	(adv) se crear la funcion ConfGrub
	(adv) se corrige HDBoot
-/var/EAC/admin/librerias/PostConf.lib
	(adv) se modifica NTDetect para que exporte SystemRoot y Windows

-/var/EAC/admin/librerias/Setting.lib
	(adv) se detecta las ip desde ifconfig eth0 y HW

-/var/www/eac
	se enlaza desde la pestaña status los puertos 1234 (tracker) 2780(servidor torrent)
 
-/var/EAC/nfsroot/testing/rc.local
	se configura temporalmente los ficheros mtab, hosts, export

ver. 0.0.7. release 4
-Servidor LINUX EAC
	(adv) software: actualizar(compilar) udp-sender 2008-09-14 y  ctorrent 3.3.2 remastersys
	(adv)rc.local (se introduce mensaje de servidor en la tty1 en rojo)
	(adv)rc.local se inicia el EACtorrent con una prioridad baja (8); 	

-/var/EAC/admin/librerias/Deploy.lib
	(adv) se formaliza algún codigo
	(adv) se crear la funcion MountRepo y UmountRepo
	(adv) se inserta una llamada SetPartitionType a MakeOffline
	(adv) se introduce control de errores en el servidor multicast en /var/EAC/admin/config/parametrosaccesoBD.php
	(adv) se quita de la funcion CreateImageFromPartition las llamadas a AmpliarFSNT y ReducirFS
-/var/EAC/admin/librerias/ATA.lib
	(adv) se crea la funcion SetPartitionType para cambiar el tipo de partion.
	(adv) se mejora la funcion SetPartitionActive, se corrige el bug de si partition activa, error.

-/var/EAC/nfsroot/{stable,testing}/etc/rc.local
	(adv) se estrucutra para determinar si el arranque es cliente pxe.

ver. 0.0.7 release 3
se corrigen bugs de la 007
se introduce nfsroottesting

version 0.0.7 01-11-08
-Servidor LINUX EAC
	(adv) se introduce variables (parametrosaccesoBD.php) para el control de Mcast
	(adv) Se adapta el sistema EAC a /var/EAC/{admin hdimages nfsroot tftpboot changelog ..}
	(adv) se elimina /etc/parametrosaccesoBD.php (sustituido por /var/EAC/admin/config/parametrosaccesoBD.php)
	(adv) se exporta el sistema nfsroot/stables como solo lectura
	(adv) se sustituyen los servicios de EAC por /var/EAC/admin/procedimientos/{EACserver EACtracker EACtorrent}	
	(adv) se añade a CREDITS un apartado para los Member contributers EAC
	(adv) se añade a todos los ficheros la cabecera de GPL y el autor del archivo 
	(adv) se crean tablas en la BD para empezar a controlar los elementos del EAC (imagenes, log ...)
-/var/www/eac:   
	(adv) se adapta la web para reconocer /var/EAC 
	(adv) recepcion del formulario particion (Particionado.php) se añÃ±ade la linea SetDefaultBoot 1 $MAC pra el fichero Particionado.sh)
	(adv) se aÃade 6 digitos para el tamaÃo de paticion en formParticionado.sh.php
	(adv) Se adaptan la pestaÃña de los servicios para usar LSB
	(adv) Se sincroniza la eliminacion de los equipos en BD y en tftpboot
	(adv) Se añade la vesión en la pagina de inicio
	(adv) Se aÃ±ade ell enlace /var/www/eac/parametrosaccesoBD.php a /var/EAC/admin/config/parametrosaccesoBD.pghp
	
-/var/EAC/nfsroot/stable:
	(adv) se elimna el fichero etc/parametrosaccesoBD.php
	(adv) se adapta a trabajar en un sistema de solo lectura
-/var/EAC/admin/procedimientos/EACserver  
	(adv) [acepta solicitud del cliente para crear torrent y despues de su creación la siver con un restart de servertorrent.sh]
	(adv) [Se descomenta la linea sleep 5]  
	(adv) acepta restauraciondirecta a la particion desde una imagen Directmcast y Directucast
-/var/EAC/admin/librerias/deploy.lib  
	(adv) se adaptan las funciones para adaptarse a /var/EAC	
	(adv) Nuevas funciones IfFileExist, IfFileNotExist, GetLastStr, RetorePartitionFromImage, DeployPartitionFromimage
	(adv) Se adaptan nombre y codigo de CrearImagenParticion, RestaurarImagenCreateImageFromPartition UpdateCache RestorePartitionFromImage	
	(adv) [se modifica la funcion CreateTorrentFromImage para realizar esta tarea el servidorEAC a través de [:Servidor.php]
	(adv) [se modifica ReducirFileSystemNT para ajustar el tamaño al tamaño de la particion + 1000Mb]
	(adv) se Redefinen DeployPartitionFromImage, RestorePartitionFromImage, UpdateCache

-/var/EAC/tftpboot/
	(jbl) se añade arranque ping, dos y partedmagic

ver. 0.0.5      22-Sep-2008
	(goc) ÃAÃ±adidos ficheros changelog, LICENSE, LICENSES, INSTALL, CREDITS

ver. 0.0.4      18 sep 2008
/EAC.files/admin/procedimientos/Servidor.php:   
	(adv) se aÃ±aden parametros start, status, stop (para su gestion web)
	(adv)	/var/www/eac/status/ (gestion servicios EAC)  servertorrent.sh y tracker.sh 

ver. 0.0.3 (adv)     18 sep 2008
webeac: (adv)se añaden los formularios para generar los codigos de las statpages

version 0.0.0 (adv)    13 sep 2008
se libera el sistema completo:
	(adv) librerias con 71 funciones especificas - ata.lib(16 funciones), postconf.lib(17 librerias), filesystem.lib(11 librerias), deploy.lib(14 librerias), settings.lib(5librerias), boot.lib(7librerias)
	(adv) EAC server web
	(adv) EAC boot agent sistema opertativo a través de nfs basado en ubuntu con soporte framebuffer
	(adv) Comunicación cliente servidor basados en colas de trabajo.
	(adv) Documentación extensa sobre EAC.server EAC.bootaget, Instalacion rápida, guia de funciones y presentación

	  
  
